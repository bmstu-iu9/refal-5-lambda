* TREE

$ENTRY Go {
  /* empty */ = /* empty */;

  X = <Test '******************************' ()>;
}

*$NOOPT Test

Test {
  '*' e.Count (e.Acc) = <Test e.Count ((e.Acc) (e.Acc))>;

  /* empty */ (e.Acc) = e.Acc;
}
